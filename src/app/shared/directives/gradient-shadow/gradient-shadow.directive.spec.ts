import { GradientShadowDirective } from './gradient-shadow.directive';

describe('GradientShadowDirective', () => {
  it('should create an instance', () => {
    const directive = new GradientShadowDirective();
    expect(directive).toBeTruthy();
  });
});
